apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: myingress
  labels:
    app: get
spec:
  ingressClassName: azure-application-gateway
  rules:
  - host: get-task.achinta.site
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: get
            port: 
              number: 80
